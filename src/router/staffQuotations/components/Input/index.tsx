import { Input } from "@/components/ui/Input"

export const SearchField = () => {
    return (
        <Input
            placeholder="Search by ..."
            // value={ }
            // onChange={}
            className="basis-1/2"
        />)
}